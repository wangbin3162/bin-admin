.bin-menu-horizontal {
  height: 64px;
  line-height: 64px;
}
.bin-menu-horizontal.bin-menu-light:after {
  display: none;
}
//  表格
.bin-table-wrapper {
  border: none;
  .bin-loading {
    border: none;
  }
}

.bin-table-small .bin-table-cell {
  font-size: 12px;
}

.bin-input-suffix {
  width: auto;
}

.bin-table {
  user-select: text;
}
